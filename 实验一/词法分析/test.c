const int array[2] = {1,2};

int main(){
    int c;
    int b = 1;
    /*
    123456
    */
    c = getint();
    printf("output is %d",c);
    if (c > 1 || b>1 || c==1 )
    {
       printf("output is %d",c); 
    }
    if (c > 1 && b>1)
    {
       printf("output is %d",c); 
    }//123456
    return c;
}
~~~
